export interface ISlides {
    /** Link to image for slide. */
    readonly img: string
    /** Link to the book shown. */
    readonly url: string
    /** Slide image description. */
    readonly alt: string
}
